# outputs/

Deterministic, reproducible artifacts written by notebooks and `src/` modules.
Not tracked in git — regenerate with the pipeline.

Convention: each notebook writes into a versioned subfolder, e.g.
`outputs/01_inventory/`, `outputs/02_dem/`, `outputs/03_chm/`, etc.
